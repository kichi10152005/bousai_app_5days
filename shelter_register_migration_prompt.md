# 避難所登録機能を実装するための一括指示プロンプト

以下の指示を1回の依頼として実行してください。あなたは、現在のシステム構成・使用フレームワーク・データベース・認証方式・画面デザインを最初に調査したうえで、既存機能を壊さずに「避難所登録」機能を実装する担当AIです。実装方式は移植先の技術に合わせてください。既存の設計・命名規則・UIデザイン・認証機構を優先し、不要な大規模リファクタリングは行わないでください。

## 目的

管理者がログイン後に避難所情報を登録できる画面と登録処理を追加してください。登録した避難所は、既存の避難所一覧・検索・APIなど、システム内で避難所データを表示する機能から利用できる状態にしてください。

## 最初に行う調査

実装前に、次の項目をリポジトリ内で確認してください。

- 現在のフロントエンド、バックエンド、ルーティング、認証方式
- 避難所データのモデル、テーブルまたはJSON等の保存先
- 既存の避難所一覧・検索・API・画像表示処理
- 既存のフォーム部品、エラーメッセージ、成功メッセージ、CSS規約
- 既存のテスト、起動方法、テスト実行方法

確認後、移植先で最も自然な実装場所に機能を追加してください。既存の認証や保存方式がある場合はそれを再利用してください。

## 必須の画面仕様

ページの見出しと説明文は次の内容にしてください。

- 見出し：`避難所登録`
- 説明：`新しい避難所情報を登録します。`

登録フォームには次の項目を、この順番で配置してください。必須項目には赤色などで目立つ `必須` 表示を付けてください。

### 1. 避難所名

- テキスト入力
- 必須
- ラベル横に例として `（例：NTTデータ関西）` を表示
- プレースホルダーにも `NTTデータ関西` を設定

### 2. 避難所住所

- 住所のテキスト入力
- 必須
- ラベル横に例として `（例：大阪府大阪市北区堂島3-1-21）` を表示
- プレースホルダーにも `大阪府大阪市北区堂島3-1-21` を設定
- 郵便番号入力欄を用意する
- 郵便番号入力欄のプレースホルダーは `郵便番号`
- 郵便番号から住所を取得する `住所検索` ボタンを配置する
- 郵便番号は数字以外の文字を除去して検索する
- 住所検索に成功した場合、取得した都道府県・市区町村・町域を住所欄へ自動入力する
- 該当住所がない場合は住所欄を空にし、`該当する住所が見つかりませんでした` と表示する
- 通信に失敗した場合は住所欄を空にし、`住所検索に失敗しました` と表示する
- 外部の郵便番号APIを使う場合は、既存プロジェクトの方針に従い、エラー処理と利用条件を確認する
- 外部APIが使えない環境でも、住所を手入力して登録できるようにする

### 3. 避難所許容人数

- テキストまたは数値入力
- 必須
- ラベル横に例として `（例：100人）` を表示
- プレースホルダーにも `100人` を設定
- 既存仕様が数値のみを求める場合は、その仕様に合わせて適切に検証する。ただし、`100人` や `約１０００人` のような既存データを扱う必要がある場合は、保存形式を壊さないこと

### 4. 避難所設備詳細

- 必須
- 複数選択可能なチェックボックス
- 次の選択肢を用意する
  - `バリアフリー設備`
  - `ペット対応設備`
  - `多言語対応`
  - `なし`
  - `その他`
- `その他` を選択したときだけ、自由入力欄を表示する
- 自由入力欄のプレースホルダーは `その他の設備を入力`
- 選択された設備と自由入力値を、既存のデータ形式に合わせて保存する
- 設備が1つも選択されていない場合は登録を拒否する

### 5. 対応災害

- 必須
- 複数選択可能なチェックボックス
- 次の選択肢を用意する
  - `地震`
  - `津波`
  - `土砂災害`
  - `河川氾濫`
  - `積雪災害`
  - `台風`
  - `その他`
- `その他` を選択したときだけ、自由入力欄を表示する
- 自由入力欄のプレースホルダーは `その他の災害を入力`
- 選択された災害と自由入力値を、既存のデータ形式に合わせて保存する
- 災害が1つも選択されていない場合は登録を拒否する

### 6. 避難所画像URL または画像ファイル

- 任意
- 画像URL入力欄を用意する
- プレースホルダーは `https://example.com/image.jpg`
- 画像ファイルのアップロード欄も用意する
- 画像ファイルは画像形式だけを受け付ける
- URLとファイルの両方が指定された場合は、既存仕様に合わせて優先順位を決め、画面またはコード上で一貫させる
- アップロード先は既存の静的ファイル領域またはメディア保存機構を利用する
- 元のファイル名をそのままパスに使わず、安全なファイル名に変換する
- 拡張子・MIMEタイプ・サイズを検証し、危険なファイルを保存しない
- 保存した画像を既存の避難所一覧や詳細画面で表示できる形式にする

## 登録処理の仕様

- 登録画面は管理者など、既存システムで定義された権限を持つユーザーだけが利用できるようにする
- 未ログインまたは権限のないユーザーは、既存のログイン画面または権限エラー画面へ遷移させる
- POST等で送信された値は、前後の空白を除去して扱う
- サーバー側でも必須項目を必ず検証する。HTMLの `required` 属性だけに依存しない
- エラー時は登録処理を行わず、どの項目が不足しているかを日本語で表示する
- 成功時は避難所データを保存し、画面上に `避難所名を登録しました。` と表示する
- 登録時に一意なIDが必要な場合は、既存の採番方式・DBの自動採番を使う
- 設備と災害の複数選択値は、既存の保存形式を維持する。既存形式が文字列の場合は、各値をカンマとスペースで連結するなど、既存データと統一する
- 保存後に避難所一覧・検索・APIで新しいデータが取得できることを確認する
- CSRF対策、入力値のエスケープ、ファイルアップロードの安全対策など、既存システムで使われているセキュリティ対策を適用する
- 二重送信やリロードによる意図しない重複登録を、既存構成に適した方法で防止する

## UI・操作性

- 既存ページのレイアウト、フォント、色、ボタン、余白に合わせる
- 必須項目、エラー、成功状態が視覚的に分かるようにする
- 画面幅が狭い場合も入力欄やボタンが画面からはみ出さないようにする
- `住所検索` や `その他` の表示切り替えは、JavaScript等で実装する場合も、要素が存在しないページでエラーにならないようにする
- `その他` の自由入力欄は、チェックを外したときに非表示にし、値を登録対象にしない
- 住所検索中・検索失敗時など、ユーザーが状態を理解できる表示を追加する
- ラベルと入力欄を正しく関連付け、キーボード操作とスクリーンリーダーにも配慮する

## テスト要件

実装後、既存のテスト方式に合わせて、少なくとも次をテストしてください。

- 未ログインで登録画面にアクセスできない
- ログイン済みユーザーが登録画面を表示できる
- 必須項目が不足している登録を拒否できる
- 設備を複数選択して登録できる
- 災害を複数選択して登録できる
- 設備の `その他` の入力値を保存できる
- 災害の `その他` の入力値を保存できる
- 画像URLを保存できる
- 画像ファイルを安全に保存できる
- 正常な登録後に成功メッセージが表示される
- 登録した避難所が一覧・検索・APIで確認できる
- 登録画面HTMLに次の例示が含まれている
  - `NTTデータ関西`
  - `大阪府大阪市北区堂島3-1-21`
  - `100人`

## 完了条件

実装だけで終了せず、次の順番で完了させてください。

1. 変更したファイルと実装内容を確認する
2. フォーマッター、リンター、型チェックがあれば実行する
3. 避難所登録機能に関係するテストを実行する
4. 可能であれば、登録画面を実際に表示して主要な操作を確認する
5. 失敗したテストがあれば、今回の変更が原因か既存の問題かを切り分ける
6. 最後に、変更したファイル、実行した検証コマンド、結果、残っている注意点を簡潔に報告する

既存ファイルにユーザーの変更がある場合は、それを上書き・削除せず、内容を確認して共存させてください。要求されていない機能や画面の改修、認証情報のハードコード、不要な依存関係の追加は行わないでください。
