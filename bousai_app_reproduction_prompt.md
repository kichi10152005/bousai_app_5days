# 防災アプリ再現用プロンプト

以下の要件をすべて満たす防災Webアプリを、この指示1回で実装してください。実装後は、起動方法、アクセスURL、変更したファイル、実行したテスト結果を簡潔に報告してください。要件に不明点があっても、以下の仕様を優先して自律的に完成させてください。

## 目的

青森市民向けに、青森市の避難所、現在発令中の気象警報・注意報、行政からの住民向け発信を確認できるWebアプリを作成する。

## 技術要件

- Python 3 と Flask を使用する。
- HTMLテンプレートは共通レイアウトを継承する構成にする。
- データはJSONファイルで管理し、避難所と発信内容を保存できるようにする。
- JSONが存在しない、または壊れている場合でもアプリが起動できるようにする。
- 外部API取得に失敗した場合は、画面を壊さず「取得失敗」と表示する。
- 外部データを使う場合は、取得失敗時に四角形などの代替区域を表示してはいけない。
- 画面とメッセージは日本語にする。

## 対象地域の固定

このアプリで表示・取得・登録する地域は青森市のみとする。

- 表示名: `青森市`
- 都道府県コード: `020000`（青森県）
- 青森市の気象庁市区町村コード: `0220100`
- 気象庁警報・注意報URL: `https://www.jma.go.jp/bosai/warning/data/r8/020000.json`
- 他の市区町村の警報・注意報を画面、地図、APIに表示しない。
- 避難所データには `city: "青森市"` を必ず持たせる。
- 避難所一覧、検索結果、避難所APIは `city == "青森市"` のデータだけを返す。
- 避難所登録時も、登録データに自動的に `city: "青森市"` を設定する。

## ホーム画面

ホーム画面には以下を表示する。

1. 防災アプリのヘッダーとナビゲーション
2. 避難所・災害マップ
3. 青森市の現在の警報・注意報一覧
4. 住民向けの発信内容があれば確認できる導線
5. 避難所検索、管理者ログインへの導線

警報・注意報の見出しには、必ず「青森市」と表示する。別地域名やダミーの地域名を表示しない。

## 地図の要件

Leaflet等の地図ライブラリを使って、青森市周辺の地図を表示する。

### 市境と水域

- 市区町村を四角形、矩形、グリッド、単純な緯度経度の箱で近似してはいけない。
- 実際の行政区域の境界GeoJSONを使用する。
- 青森市の境界データ例: `https://raw.githubusercontent.com/niiyz/JapanCityGeoJson/master/geojson/02/02201.json`
- GeoJSONの `MultiPolygon` を正しく表示できるようにする。
- 市町村界は濃い色で、くっきりとした境界線として常時表示する。
- 海、湖、河川などの水域を着色しない。境界GeoJSONの陸域ポリゴンの内部だけを塗る。
- 背景の地図タイル、道路、地名、避難所マーカーが見えるようにする。
- 四角い枠線、四角い背景、背景グリッド、四角い区域分けを地図上に描画しない。

### 塗り分け

- 非発令地域は完全に透明にする。通常時用の薄い緑なども塗らない。
- 現在「発表」または「継続」の警報・注意報が存在する区域だけを塗る。
- 市区町村または二次細分区域の発令情報を使い、該当する青森市の区域だけを塗る。
- 発令レベルは同じ区域の情報のうち最も高いものを採用する。
- 色はコントラストが高く視認性のある濃いトーンにする。
- 推奨色:
  - 注意報: `#f2c94c`
  - 警報: `#e45756`
  - 危険警報: `#8e44ad`
  - 特別警報: `#263238`
- 塗りの不透明度は地図を隠さない範囲で設定するが、発令地域であることが明確に分かるようにする。
- 境界線は例として `#172554`、太さ `2px`、不透明度 `0.95` 程度にする。

### 避難所

- 青森市の避難所だけを地図上に表示する。
- 緯度・経度がある避難所は地図上に地点マーカーを表示する。
- マーカーをタップすると避難所名を表示する。
- 市区町村の区域表示を四角いマーカーで代用してはいけない。避難所の地点マーカーは使用してよい。

## 気象警報・注意報

バックエンドで気象庁のJSONを取得し、青森市の情報だけを抽出する。

- `class20Items` の中から `areaCode == "0220100"` の区域だけを処理する。
- `status` が `発表` または `継続` のものだけを対象にする。
- `code == "00"`（解除）は表示しない。
- 警報・注意報名、コード、状態、対象区域、市区町村コードを返す。
- 発表時刻を日本時間で表示する。
- 取得失敗時はエラー状態を返し、警報がない状態と混同しない表示にする。
- 10分ごとに最新情報を自動更新する。

警報コードの重要度判定は次を基本とする。

- `02`〜`09`: 警報
- `10`〜`29`: 注意報
- `32`〜`39`: 特別警報
- `43`, `48`, `49`: 危険警報

一覧表には少なくとも「日時」「内容」「対象地域」「重要度」を表示する。

- 注意報の重要度は「中」とする。
- 警報以上の重要度は「大」とする。
- 「大」は赤色で、通常の文字より大きく、太字で目立たせる。

## 青森市タップ時の表示

青森市の行政区域をタップした場合、その時点で青森市に発令中の警報・注意報をすべてポップアップで表示する。

ポップアップには以下を含める。

- 青森市
- 警戒レベル
- 発令中の警報・注意報を全件
- 各項目の名称
- 対象区域（二次細分区域があればその名称）
- 発表または継続の状態

発令中の項目は赤系の見出し、太字、区切りで目立つようにする。発令がない場合は「現在、警報・注意報は発表されていません」と表示する。ポップアップの内容を固定文言1件だけにしてはいけない。

## 管理者機能

管理者ログインを実装する。

- 避難所登録と発信ボードはログイン必須にする。
- 未ログインで保護ページへアクセスした場合、ログイン画面へリダイレクトする。
- ログイン後は元のページへ戻せるようにする。
- 外部サイトへリダイレクトできるオープンリダイレクトを作らない。
- ログアウト機能を用意する。
- パスワードなどの認証情報は本番環境では環境変数や安全な認証基盤を使える構成にする。

## 避難所登録・検索

- 管理者が避難所を登録できる。
- 避難所名、住所、許容人数、設備詳細、対応災害を入力できる。
- 避難所名の例として `NTTデータ関西` を表示する。
- 住所の例として `大阪府大阪市北区堂島3-1-21` を表示する。
- 許容人数の例として `100人` を表示する。
- 避難所住所は郵便番号入力と住所検索ボタンに対応させ、外部APIが使えない場合も手入力できるようにする。
- 設備詳細は複数選択式にし、`バリアフリー設備`、`ペット対応設備`、`多言語対応`、`なし`、`その他` を選べるようにする。
- 対応災害は複数選択式にし、`地震`、`津波`、`土砂災害`、`河川氾濫`、`積雪災害`、`台風`、`その他` を選べるようにする。
- `その他` 選択時は自由入力欄を表示する。
- 画像URL入力と画像ファイルアップロードに対応する。ファイル名、形式、MIMEタイプ、サイズを検証し、安全なファイル名で保存する。
- 避難所名が空の場合は登録せずエラーメッセージを表示する。
- その他の必須項目もサーバー側で検証し、不足時は登録しない。
- IDは既存最大IDに1を加えて生成する。
- 登録時に青森市の所属情報を自動付与する。
- 一覧ページと検索結果ページを用意する。
- `GET /shelters` のようなJSON APIを用意する。
- 地区指定がある場合は、青森市のデータの中からさらに地区で絞り込む。
- 該当データがない場合は、画面に適切なメッセージを表示し、APIは適切な404レスポンスを返す。

## 発信ボード

- 管理者が登録した発信内容を保存できる構成にする。
- 住民向けの発信だけを住民向け画面に表示する。
- 発信内容に避難先、状態、作成日時、更新日時を持たせられるようにする。
- 発信がない場合は空状態を表示する。

## デザイン要件

- 防災用途なので、情報を素早く比較できる明快なUIにする。
- 警報などの重要情報は色、太字、サイズ、余白で明確に強調する。
- 地図上の色は濃く視認性を高めるが、道路や地名が判読できるようにする。
- 地図上に四角い枠線、背景グリッド、四角い区切りを一切追加しない。
- モバイル画面でもテーブル、ポップアップ、ナビゲーションが崩れないようにする。
- 既存のデザインシステムがある場合はそれに合わせる。

## 推奨ルート

- `GET /`: ホーム画面
- `GET /login`: 管理者ログイン
- `GET /logout`: ログアウト
- `GET, POST /shelter_register`: 避難所登録
- `GET /shelter_search`: 避難所検索画面
- `GET /all_shelters`: 青森市の全避難所一覧
- `GET /search_results`: 避難所検索結果
- `GET /shelters`: 青森市の避難所JSON API
- `GET /board`: 住民向け発信ボード
- `GET /api/weather_warnings`: 青森市の警報・注意報JSON API

## 完了条件

実装後、次を必ず確認する。

1. アプリが起動し、ホーム画面がHTTP 200で表示される。
2. 青森市以外の避難所が一覧・検索結果・APIに含まれない。
3. 青森市以外の警報・注意報がAPI、一覧、地図、ポップアップに含まれない。
4. 警報・注意報がない場合、非発令地域が透明である。
5. 発令時だけ対象区域が塗られ、塗り色が濃く視認できる。
6. 市境が実際の行政区域形状に沿って表示され、矩形ではない。
7. 海や湖などの水域が着色されない。
8. 青森市をタップすると、発令中の内容が全件表示される。
9. 重要度「大」が赤色・大きな文字・太字で表示される。
10. 避難所登録時に `city: "青森市"` が保存される。
11. 未ログイン状態で管理者ページが保護される。
12. Pythonの構文チェック、JavaScriptの構文チェック、主要ルートの動作確認を実行する。

余計な地域、架空の市区町村名、矩形の代替地図、不要な機能は追加せず、上記の青森市向け防災機能を完成させてください。
